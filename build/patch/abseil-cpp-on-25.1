diff --git a/absl/container/internal/btree.h b/absl/container/internal/btree.h
index 569faa07..da481f06 100644
--- a/absl/container/internal/btree.h
+++ b/absl/container/internal/btree.h
@@ -1585,7 +1585,7 @@ class btree {
   }
 
   // Verifies the structure of the btree.
-  void verify() const;
+  void btree_verify() const;
 
   // Size routines.
   size_type size() const { return size_; }
@@ -2606,7 +2606,7 @@ void btree<P>::swap(btree &other) {
 }
 
 template <typename P>
-void btree<P>::verify() const {
+void btree<P>::btree_verify() const {
   assert(root() != nullptr);
   assert(leftmost() != nullptr);
   assert(rightmost() != nullptr);
diff --git a/absl/container/internal/btree_container.h b/absl/container/internal/btree_container.h
index a68ce445..4662c7ca 100644
--- a/absl/container/internal/btree_container.h
+++ b/absl/container/internal/btree_container.h
@@ -221,7 +221,7 @@ class btree_container {
   // Utility routines.
   ABSL_ATTRIBUTE_REINITIALIZES void clear() { tree_.clear(); }
   void swap(btree_container &other) { tree_.swap(other.tree_); }
-  void verify() const { tree_.verify(); }
+  void container_verify() const { tree_.btree_verify(); }
 
   // Size routines.
   size_type size() const { return tree_.size(); }
